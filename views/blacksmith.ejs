<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Cinzel|Oswald|Playfair+Display&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/nav-style.css">
    <link rel="stylesheet" href="/body-general.css">
    <link rel="stylesheet" href="/blacksmith.css">
    <title>The Blessed Adventure | Blacksmith</title>

</head>

<body>
    <%- include('./partials/navbar.ejs') %>

    <div class="judul">
        <h1>Blacksmith</h1>
        <h3>Create equipments you needs here.</h3>
    </div>
    <div class="container">
        <div class="content">
            <div class="table">
                <table>
                    <h2 class="mymaterials">My Materials:</h2>
                    <br>
                    <% if (Object.keys(relatedItems).length == 0) { %>
                    <h2>You have no related Items</h2>
                    <%} else {%>
                    <tr>
                        <th>Item's name</th>
                        <th>count</th>
                    </tr>

                    <% for (let item in relatedItems) { %>
                    <tr>
                        <td><%= item %></td>
                        <td><%= relatedItems[item].count %> X</td>
                    </tr>
                    <%}%>
            
                        <%}%>
                </table>
            </div>

            <div class="table">
                <table>
                    <tr>
                        <th>Illustration</th>
                        <th>Item's name</th>
                        <th>Materials needed</th>
                        <th>Option</th>
                    </tr>

                    <% for (let i = 0; i < items.length; i++) { %>
                    <tr class="item">
                        <td><img src="" alt="belom ada"></td>
                        <td><%= items[i].dataValues.name %></td>

                        <% if (items[i].dataValues.name == 'Dagon') { %>
                        <td>
                            (3x) dragon_scale <br>
                            (2x) dragon_arm <br>
                        </td>
                        <% } else if (items[i].dataValues.name == 'Chrono') { %>
                        <td>
                            (6x) dragon_scale <br>
                            (4x) dragon_arm <br>
                        </td>
                        <% } else if (items[i].dataValues.name == 'Elixir') { %>
                        <td>
                            (3x) dragon_eyes <br>
                            (2x) dragon_legs <br>
                        </td>
                        <% } else if (items[i].dataValues.name == 'Cryo') { %>
                        <td>
                            (6x) dragon_eyes <br>
                            (4x) dragon_legs <br>
                        </td>
                        <%}%>
                
                        <td><a class="craft" href="/blacksmith/<%= user.username %>/craft/<%= items[i].dataValues.name %>">CRAFT!</a>
                        </td>

                    </tr>
                    <%}%>
                </table>
            </div>
        </div>
    </div>

</body>

</html>